# SDK Generation (later F1)

TypeScript SDK generation configuration (Appendix B). Deferred until the
OpenAPI contract stabilizes; adopting the TS toolchain updates DEC-0005 or
records a successor decision.
