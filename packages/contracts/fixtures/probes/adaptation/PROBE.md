# PROBE-ADAPTATION — adaptation

Source: owner pattern program, section D (Alternate canon and adaptation branches).

Generic capability tested: adaptation branches with declared ancestry, source pinning, explicit overrides, composite characters, and approved omissions — without source mutation.

Belongs in the reusable domain model: branch_metadata (branch_type, parent_branch, pinned_source_release_ref, overrides, composites, omissions) on canon releases; production-local facts for adaptation-only truths; the retcon/fork/adaptation/production-local distinction expressed through branch_type plus fact classification.

Template/policy/adapter-specific: per-medium adaptation conventions (audio composites for performance economy, youth-audience content policy).

Must-tests carried: ancestry, inherited canon, explicit overrides, composites, omissions, source pinning (MET-F1 fork test verifies the source's canonical hash is unchanged).
Deferred to F3 kernel: impact analysis when the pinned source later changes; automatic staleness marking of adaptations.
