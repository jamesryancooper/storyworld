{
  "schema_version": "harness.project.v1",
  "project": {
    "id": "storyworld-platform",
    "name": "Storyworld Platform",
    "repository_root": ".",
    "profile": "high-assurance",
    "blueprint_version": "1.0.1",
    "adoption_status": "adopted",
    "adoption_decision_ref": "DEC-0001"
  },
  "paths": {
    "source": [
      "packages",
      "apps",
      "infra"
    ],
    "tests": [],
    "generated": [
      ".agent/generated"
    ],
    "instruction_roots": [
      "AGENTS.md"
    ],
    "fingerprint_exclusions": [
      {
        "path": "node_modules",
        "reason": "Third-party dependency tree, fully reproducible from the committed pnpm-lock.yaml (which remains fingerprinted). Hoisted linker (DEC-0009) keeps this a single real directory with no symlink forest. Excluded from source fingerprint and scans."
      },
      {
        "path": "packages/domain/node_modules",
        "reason": "Per-package third-party/bin directory pnpm or tooling may materialize inside the workspace package; reproducible from the fingerprinted lockfile. Added per the workspace convention: every new package adds its node_modules path here."
      },
      {
        "path": "packages/persistence/node_modules",
        "reason": "Per-package third-party/bin directory pnpm or tooling may materialize; reproducible from the fingerprinted lockfile (workspace convention in AGENTS.md)."
      },
      {
        "path": "packages/storage/node_modules",
        "reason": "Per-package third-party/bin directory pnpm or tooling may materialize; reproducible from the fingerprinted lockfile (workspace convention in AGENTS.md)."
      },
      {
        "path": "packages/portability/node_modules",
        "reason": "Per-package third-party/bin directory pnpm or tooling may materialize; reproducible from the fingerprinted lockfile (workspace convention in AGENTS.md)."
      },
      {
        "path": "packages/kernel/node_modules",
        "reason": "Per-package third-party/bin directory pnpm or tooling may materialize; reproducible from the fingerprinted lockfile (workspace convention in AGENTS.md)."
      },
      {
        "path": "apps/engine-api/node_modules",
        "reason": "Per-package third-party/bin directory pnpm or tooling may materialize; reproducible from the fingerprinted lockfile (workspace convention in AGENTS.md)."
      },
      {
        "path": "apps/cli/node_modules",
        "reason": "Per-package third-party/bin directory pnpm or tooling may materialize; reproducible from the fingerprinted lockfile (workspace convention in AGENTS.md)."
      },
      {
        "path": "packages/providers/node_modules",
        "reason": "Per-package third-party/bin directory pnpm or tooling may materialize; reproducible from the fingerprinted lockfile (workspace convention in AGENTS.md)."
      }
    ]
  },
  "commands": {
    "project_test": {
      "status": "configured",
      "run": "python -B packages/contracts/tests/validate_contracts.py"
    },
    "project_lint": {
      "status": "configured",
      "run": "pnpm -r lint"
    },
    "project_build": {
      "status": "configured",
      "run": "pnpm -r typecheck"
    },
    "project_closure": {
      "status": "configured",
      "run": "bash infra/scripts/ship-check.sh"
    }
  },
  "extensions": {
    "registry": ".agent/extensions/registry.json",
    "registry_required_in_profile": "standard_or_higher"
  },
  "mutable_work_status": "prohibited_here"
}
