# Proposed Amendment: `storyworld-runtime-adapter`

**Version bump:** minor  
**Source records:** EXT-051  
**Decision requirement:** Reference addition only until POC decision.

This is a proposal-only set of references. Merge only after checking the current skill version and resolving collisions.

## Trigger change

Add programmatic video or browser renderer tasks.

## Workflow change

Add a renderer-profile POC before dependency selection.

## Output change

Require renderer version, source hash, deterministic inputs, output hash, and portability result.
