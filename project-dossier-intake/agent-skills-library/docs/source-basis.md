# Source Basis

## Storyworld revision

The library was aligned to Storyworld commit:

`fc9b75b8ae3f28b4b9e13f5c0e31e9f2d24ad565`

Key repository conventions assessed:

- root `AGENTS.md`;
- `.agent/START_HERE.md`;
- `.agents/README.md`;
- `.agent/schemas/harness-capability-records.schema.json`;
- existing `change-review` and `storyworld-ux` skills;
- adopted `storyworld-steward` agent;
- unadopted `safe-change` workflow.

## Architecture package

The package in `references/integration-architecture/` is a read-only copy of the final integration architecture delivered in this conversation. It contains:

- definitive architecture report;
- current-state/conflict register;
- decision successor plan;
- contract/schema backlog;
- POC program;
- dependency roadmap;
- dossier-intake expansion plan;
- source/evidence register.

The architecture package is staged product-direction evidence. It is not accepted repository authority until Storyworld governance dispositions it.

## Open-source research

`references/open-source-research/` contains the repository study used to inform bounded dependencies, standards profiles, external-tool boundaries, and cautionary cases.

## Existing capabilities

The skill library expects to coexist with Storyworld's existing capabilities rather than duplicate them. Their exact status must be rechecked at installation time.
