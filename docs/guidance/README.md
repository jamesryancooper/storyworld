# Guidance Documents

Owner-supplied implementation guidance, stored verbatim and treated as
analyzed input (not authority): each document's use is governed by its
dossier source record (machine-readable/sources.json).
